Jinang:
- Eine obere Titelleiste erstellen und den Zurück-Button in der Tabelle-Seiten entfernen
(Die Titelleiste sollte den Startseite, Anzeige und Erstellung für alle vier Tabellen, sowie Ausloggen enthalten)
- Die Anlegen-Seite ändern, um der Logik der Detail-Seiten zu entsprechen
(d.h. bei Klicken auf die Schaltfläche "Anzeigen" einer bestimmten Zeile, Daten anzeigen und ändern können)
- Suchfunktion für jede Tabelle hinzufügen

Zijian:
- equals() und hashCode() überschreiben (Composite-id class)
- Business-Klassen erstellen
- Marken und Kategorien in Enum-Typen bzw. Aufzählungstypen ändern
- Für jede Business-Klasse entsprechende CRUD Methoden erstellen (beachten, dass nicht alle Daten gelöscht werden können)

Zihao:
- Eine Login-Seite erstellen
- Die Logik der Authentifizierung implementieren
- Bestimmte unnötige Spalten aus der Anzeigetabelle löschen
- Die xhtml-Dateien ändern, um auf der rechten Seite jeder Zeile zwei Schaltflächen (Anzeigen und Löschen) anzuzeigen
- Das Layout der Hauptseite ändern (Bild + Fußzeile)
- Logik für die Benutzerabmeldung implementieren
- Seiten für Marken und Kategorien erstellen
- README.md für Iteration3 anpassen

Felix:
- Eingabe von Produkt- oder Kundennamen mit Dropdown, das alle passenden Einträge für die aktuelle Eingabe anzeigt